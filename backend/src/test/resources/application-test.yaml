# Test profile — in-memory H2 in PostgreSQL compatibility mode.
# Replaces the real PostgreSQL datasource so JPA-backed tests and full
# SpringBootTests run without an external database.
spring:
  datasource:
    url: jdbc:h2:mem:occupi-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

# Full-context tests must not reach out to a real InfluxDB (or create caches on a
# developer's running compose stack as a side effect).
influxdb:
  last-cache:
    enabled: false

logging:
  level:
    com.occupi: DEBUG
